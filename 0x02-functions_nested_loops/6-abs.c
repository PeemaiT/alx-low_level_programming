#include "main.h"

/**
 * abs - computer the absolute value of an integer
 * @t: The integer to be computed
 * Return: The absolute value
 */

int _abs absolute value(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
